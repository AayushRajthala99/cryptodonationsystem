<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/register.css">
    <title>Registration | CDS</title>
</head>

<body>
    <div class="body-container">
        <div class="art-container">
            <img src="/img/register/registration-art.png" alt="registration-art">
        </div>
        <div class="registration-form">
            <h1>FILL OUT FORM TO REGISTER!</h1>
            <form id="registrationform" action="/register" method="post">
                <div class="registration-form-input">
                    <div class="label-container" id="labelcontainerfullname">
                        <div><label for="fullname">FULL NAME</label></div>
                        <div class="form-error">
                            <% if(errorMessage.fullname) { %>
                            <%= errorMessage.fullname %>
                            <% } %>
                        </div>
                    </div>
                    <input type="text" name="fullname" id="fullname" value="<%= result.fullname %>">
                </div>
                <div class="registration-form-input">
                    <div class="label-container" id="labelcontaineremail">
                        <div><label for="email">EMAIL</label></div>
                        <div class="form-error">
                            <% if(errorMessage.email) { %>
                            <%= errorMessage.email %>
                            <% } %>
                        </div>
                    </div>
                    <input type="text" name="email" id="email" value="<%= result.email %>">
                </div>
                <div class="registration-form-input">
                    <div class="label-container" id="labelcontainerpassword">
                        <div><label for="password">PASSWORD</label></div>
                        <div class="form-error">
                            <% if(errorMessage.password) { %>
                            <%= errorMessage.password %>
                            <% } %>
                        </div>
                    </div>
                    <input type="password" name="password" id="password" value="<%= result.password %>">
                </div>
                <div class="registration-form-input">
                    <div class="label-container" id="labelcontainerconfirmpassword">
                        <div><label for="confirmpassword">CONFIRM PASSWORD</label></div>
                        <div class="form-error">
                            <% if(errorMessage.confirmpassword) { %>
                            <%= errorMessage.confirmpassword %>
                            <% } %>
                        </div>
                    </div>
                    <input type="password" name="confirmpassword" id="confirmpassword" value="<%= result.confirmpassword %>">
                </div>
                <div>
                    <button type="submit" class="submit-button">REGISTER</button>
                </div>
            </form>
            <h2>ALREADY HAVE AN ACCOUNT? <a href="/">LOGIN</a></h2>
        </div>
    </div>
    <script src="/js/register.js"></script>
</body>

</html>